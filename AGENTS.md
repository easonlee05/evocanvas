# AGENTS.md — EvoCanvas 工作规则

> 适用工具：Claude Code、Codex、Antigravity 及所有 AI 编程工具。
> 本仓库当前以 `docs/vision/EvoCanvas1.0-PRD.md` 作为产品真相源。
> 本仓库不是全新重写，而是在 Evoloop 基础设施上做半重构迁移。

---

## 工作规则

**不要先通读整个仓库。** 按以下顺序读：

1. 本文件：职责边界、禁止事项、验证方式
2. `docs/vision/EvoCanvas1.0-PRD.md`：当前产品真相源与版本路线
3. 用 `rg` 定位与当前任务直接相关的页面、组件、后端服务或工具文件

**重要：**

- 当前产品目标是 `EvoCanvas 1.0`，不是沿用 `EvoLoop`、`manual`、`prd` 的旧产品语义。
- 修改代码时，**必须同步更新注释与 Docstrings**，确保代码注释与当前产品方向一致。
- 如果旧文案、旧变量名、旧注释仍保留 `EvoLoop`、`PRD 生成器`、`任务大厅` 等语义，应在相关改动中一并清理。
- 迁移时优先复用底座，不要为了“看起来全新”而无谓抛弃通用基础设施。

**默认不读：**

- `.env`：可能含凭证
- `dist/`：构建产物
- 无关的历史草稿或用户个人材料

---

## 当前产品目标

EvoCanvas 1.0 是一个面向产品经理的对话驱动工作台。

当前首版只验证以下最小闭环：

`输入编译 -> 待澄清问题 -> 约束 / 待决策 -> 结构化交接物`

这意味着：

- 首版首先解决“多源混杂输入如何收敛”，而不是“如何生成一份漂亮文档”
- AI 首先要像产品同事一样暴露不确定性，而不是抢先下结论
- 交付目标首先是“结构化交接物”，而不是完整 PRD、完整项目管理系统或完整交付平台

---

## 迁移原则

当前阶段是从 Evoloop 向 EvoCanvas 的半重构迁移，不是完全重写。

优先复用：

- 通用前端组件、基础布局、Workspace 壳
- 后端任务模型、事件模型、工具层、会话层、MCP、CLI、通用 runtime
- 可独立成立的基础设施测试

参考保留：

- 暂不纳入主路径、但未来可能继续复用的旧后端能力，统一放入 `reference/`
- `reference/` 只作为迁移参考，不默认视为当前实现边界

优先剥离：

- 明显服务于旧产品面的页面与导航
- 明显绑定 `manual / prd / spec_to_agent / acceptance_review` 产品语义的对象与文案
- 仅为旧产品线成立的页面级交互与展示结构

---

## 前端协作规则

- 前端当前主维护对象是：
  - `frontend/src/pages/LandingPage/`
  - `frontend/src/pages/Workspace/`
  - `frontend/src/components/`
  - `frontend/src/api.js`
- 当前仓库里若仍保留旧页面或旧组件，只能作为迁移过渡资产，不应继续扩展其产品能力。
- 不要无故扩展出 `任务大厅`、`知识库`、`法则审核`、`回收站` 这类旧产品页面。
- 如果需要增加新页面，必须先确认它属于 `1.0` 必须范围，而不是后续版本能力提前泄漏。
- 可以优化样式，但不能把产品形态改回“表单系统”“文档生成器”或“复杂后台导航”。

---

## 后端协作规则

- `app/core`、`app/services`、`app/api`、`app/mcp`、`app/cli` 默认视为可复用底座。
- 如果某个模块仍带有旧产品命名，不要直接删除；优先判断它是：
  1. 纯通用基础设施，可重命名后继续复用；
  2. 旧产品流程适配层，应逐步替换；
  3. 明显无复用价值，才考虑移除。
- 不要在没有替代方案的前提下，破坏现有任务、事件、工具、存储、会话等基础能力。

---

## AI 协作规则

任何 AI 驱动行为都应服从以下顺序：

1. 先暴露不确定性
2. 再沉淀约束
3. 再形成待决策
4. 最后生成结构化交接物

如果输入之间存在冲突、歧义或上下文缺失：

- 必须显性化冲突
- 不得静默合并
- 不得假装已经形成稳定结论

---

## 表达与文档规则

- 面向项目讨论、方案说明、架构文档、注释更新时，默认使用中文表达。
- 如需保留英文术语，采用“中文在前，英文放括号里”的形式，例如：结构化交接物（structured handoff）。
- 不要只写英文术语而不补中文，除非该内容是代码标识符、协议字段、命令、路径或外部接口原文。
- 当中英文同时出现时，应以中文为主，英文只作为辅助理解，不应让英文主导句子可读性。
- 新增或更新 harness、架构、流程类文档时，优先保证中文读者可直接阅读，不依赖英文术语才能理解。

---

## 快速验证

```bash
npm --prefix frontend run build
```

本地联调：

```bash
npm --prefix frontend run dev
```

如需接后端：

```bash
python3 -m uvicorn app.api.server:app --host 127.0.0.1 --port 8000 --reload
VITE_API_BASE=http://127.0.0.1:8000 npm --prefix frontend run dev
```
