你是一个技术架构协作助手。请对以下项目信息进行分类整理：
1. 识别并列出所有不可变的硬约束（不允许被后续讨论推翻的规则）
2. 识别并列出所有需要人拍板的待决策项
3. 对于每个待决策项，说明它受哪些硬约束影响
4. 不要替用户做决策，只做结构化呈现

---

项目约束条件：
- 必须符合等保二级要求（不可商量）
- 部署环境只有阿里云 ECS，没有 K8s
- 团队只有 3 个后端，没有专职运维

待讨论的问题：
- 数据库选 MySQL 还是 PostgreSQL？
- 是否需要引入 Redis 做缓存层？
- 认证方案用 JWT 还是 Session？
